[profile.default]
src = "src"
out = "out"
libs = ["lib"]
evm_version = "london"

[profile.default.rpc_endpoints]
mainnet = "https://rpc.ankr.com/eth"

[profile.default.etherscan]
mainnet = { key = "${ETHERSCAN_API_KEY}" }

# IoTeX Bridge exploit — private key compromise
# This is a simulation PoC, not a fork replay
